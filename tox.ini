[tox]
envlist = py3
skipsdist = true

[testenv]
deps =
    pytest>=7.0,<9.0
    pytest-asyncio>=0.21,<1.0
    aiohttp>=3.8,<4.0
    reportlab>=4.0,<5.0
    svglib>=1.5,<2.0
    lxml>=4.9
commands =
    pytest {posargs:-v}
